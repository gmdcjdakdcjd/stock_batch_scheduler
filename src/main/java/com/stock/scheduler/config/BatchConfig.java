package com.stock.scheduler.config;

import org.springframework.context.annotation.Configuration;

@Configuration
public class BatchConfig {
    // 추후 Batch 관련 Bean 설정 넣을 자리 (지금은 비워둠 )
    // 스케줄러 관련 설정
    // 스프링 배치(Spring Batch) 사용 시 JobRepository, JobLauncher 설정
}